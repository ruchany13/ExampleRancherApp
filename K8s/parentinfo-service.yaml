apiVersion: v1
kind: Service
metadata:
  name: parentinfo-service
spec:
  type: ClusterIP
  
  selector:
    app: ParentInfoApp

  ports:
    - name: http-port-mapping
      port: 80 
      targetPort: 8000 
      protocol: TCP